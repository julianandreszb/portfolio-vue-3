interface IRatingBarItem {
  id: string
  label: string
  currentRating: number
  maxRating: number
}

/*interface IRatingBar {
  items: Array<IRatingBarItem>
}*/

export type { /*IRatingBar,*/ IRatingBarItem }
